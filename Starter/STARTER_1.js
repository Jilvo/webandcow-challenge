// Starter Challenge : Variables et calculs : https://tainix.fr/challenge/Variables-et-calculs
function perimeter(side) {
  return side * 4;
}

function area(side) {
  return side * side;
}
const side = 46;
const resultat = perimeter(side) + area(side);
console.log(resultat);
